__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Rebuilt deterministically by scripts/build_warehouse.py (fixed seed), so
# there is no reason to carry 3.5 MB of binary in the repository.
data/*.duckdb
data/*.duckdb.wal

# Your uploaded files and app state stay on your machine.
data/uploads/
data/app_state.json

# Downloaded by eval/user_data/run.py; public but large.
data/user_eval/
